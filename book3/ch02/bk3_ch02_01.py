import numpy as np

# define a column vector
a_col = np.array([[1], [2], [3]])

b_col = 2 * a_col

# define a row vector
a_row = np.array([[1, 2, 3]])

b_row = 2 * a_row

# define a matrix
A = np.array([[1, 2, 3],
              [4, 5, 6]])

B = 2 * A
